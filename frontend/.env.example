# Base URL of the OpenMEP FastAPI backend.
# Leave empty to call the same origin (works with the dev proxy, or a reverse
# proxy that serves this SPA and forwards /api to the backend).
# For a separate API host in production, set e.g.:
#   VITE_API_BASE=https://api.your-domain.com
VITE_API_BASE=
